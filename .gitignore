# Never commit tokens / creds
*.token
mesh-token.txt

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
# NOTE: .terraform.lock.hcl is intentionally COMMITTED (HashiCorp guidance) so
# provider versions are reproducible across machines and CI.

# Local editor / OS junk
.DS_Store
*.swp
